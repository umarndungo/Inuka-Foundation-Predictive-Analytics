# Copy to .env and fill in only what you need for live SMS.
# Compose loads .env automatically for ${VAR} substitution.

# --- Twilio / n8n (optional for demo; /evaluate still works without them) ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
FIELD_WORKER_PHONE_NUMBER=

# --- Frontend (host) ---
# NEXT_PUBLIC_API_URL=http://localhost:8000
# NEXT_PUBLIC_USE_MOCK=false
